name: esc_pos_utils
description: Basic Flutter/Dart classes for ESC/POS printing. Ticket class generates ESC/POS commands that can be sent to a thermal printer.
version: 0.3.7
homepage: https://github.com/andrey-ushakov/esc_pos_utils

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  hex: ^0.2.0
  gbk_codec:
    git:
      url: https://github.com/maxleexyz/gbk_codec.git
  image: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
